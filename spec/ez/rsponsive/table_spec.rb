require 'spec_helper'

describe Ez::Rsponsive::Table do
  it 'has a version number' do
    expect(Ez::Rsponsive::Table::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
